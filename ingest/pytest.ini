[pytest]
# `rootdir`-relative import of the `app` package, the same way uvicorn sees it.
pythonpath = .
testpaths = test_ingest.py
# -q keeps a green run to a few lines; failures still print in full.
addopts = -q
